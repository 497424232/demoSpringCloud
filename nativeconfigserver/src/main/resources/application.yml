server:
  port: 8762
#当前服务在注册中心的名字
spring:
  application:
    name: nativeconfigserver
#配置文件的获取方式，native表示本地获取
  profiles:
    active: native
#本地配置文件存放路径
  cloud:
    config:
      server:
        native:
          search-locations: classpath:/shared

